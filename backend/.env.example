# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379

# Uncomment if Redis requires authentication
# REDIS_PASSWORD=your_password

# Uncomment for Redis Cluster or specific database
# REDIS_DB=0
